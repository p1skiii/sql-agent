# Changes Summary

- Updated `tests/conftest.py` to rename `agent_ctx` to `read_agent_ctx`, keeping the fixture focused on read pipeline tests with `AlwaysReadIntentModel` for intent, `FakeSqlModel` for fake mode, real LLM builder for non-fake, and normalized config (`max_rows` defaulted to 10, `allow_trace=True`).
- Moved the read pipeline integration test to `tests/test_read_pipeline.py` and adjusted it to consume `read_agent_ctx`, explicitly pass `IntentType.READ_SIMPLE`, and retain existing assertions for SQL shape, rows, columns, summary, and trace.
- Attempted `uv run pytest --model fake`; build failed before tests due to missing `README.md` referenced by the build backend, so no tests were executed. Please add the README or adjust packaging to enable test run.
